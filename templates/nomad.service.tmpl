[Unit]
Description=Nomad
After=consul.service 
Requires=consul.service

[Service]
ExecStart=/usr/bin/nomad agent {{.Flags}}
ExecReload=/bin/kill -HUP $MAINPID
LimitNOFILE=65536

[Install]
WantedBy=multi-user.target
